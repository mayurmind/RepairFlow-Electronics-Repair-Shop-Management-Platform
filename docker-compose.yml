version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: repairflow-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
      POSTGRES_DB: repairflow
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
